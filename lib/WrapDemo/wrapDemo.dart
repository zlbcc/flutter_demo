import 'package:flutter/material.dart';

class WrapDemos extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('WrapDemos'),
      ),
      body: Container(
        child: Text('data'),
      ),
    );
  }
}
